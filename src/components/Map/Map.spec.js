/* @flow */

/* import React from 'react';
import { describe, it } from 'mocha';
import { expect } from 'chai';
import { shallow } from 'enzyme';

import CustomMap from './Map';


const setup = () => {
	const props = {
		cities: [
			{
				name: 'Paris',
				latitude: 48.856614,
				longitude: 2.352222,
			},
			{
				name: 'Versailles',
				latitude: 48.804865,
				longitude: 2.120355,
			},
		],
		bounds: {
			getBounds: () => ({
				_southWest: {
					lat: 48.808026,
					lng: 2.192418,
				},
				_northEast: {
					lat: 48.856614,
					lng: 2.352222,
				},
			}),
		},
	};

	return shallow(<CustomMap {...props} />);
};

describe('test CitiesList Component', () => {
	it('renders list of ListItem', () => {
		const wrapper = setup();
		expect(wrapper.find('Map').length).to.equal(1);
	});
}); */
